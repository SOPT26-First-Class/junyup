function addNum(x, y) {
    console.log(x + y);
}
addNum(2, 3);

const addStr = function(x, y) {
    console.log(x + y);
}
addStr('함수', '표현식');

const addBool = (x, y) => {
    console.log(x + y);
}
addBool(true, false);
